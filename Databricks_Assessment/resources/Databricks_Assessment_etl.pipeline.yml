# The main pipeline for Databricks_Assessment

resources:
  pipelines:
    Databricks_Assessment_etl:
      name: Databricks_Assessment_etl
      catalog: ${var.catalog}
      schema: ${var.schema}
      serverless: true
      root_path: "../src/Databricks_Assessment_etl"

      libraries:
        - glob:
            include: ../src/Databricks_Assessment_etl/transformations/**

      environment:
        dependencies:
          # We include every dependency defined by pyproject.toml by defining an editable environment
          # that points to the folder where pyproject.toml is deployed.
          - --editable ${workspace.file_path}
