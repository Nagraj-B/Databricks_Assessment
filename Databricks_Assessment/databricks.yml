# This is a Databricks asset bundle definition for Databricks_Assessment.
# See https://docs.databricks.com/dev-tools/bundles/index.html for documentation.
bundle:
  name: Databricks_Assessment
  uuid: aea2c35d-0fd0-483d-9ee4-2cac44197ded

include:
  - resources/*.yml

# Variable declarations. These variables are assigned in the dev/prod targets below.
variables:
  catalog:
    description: The catalog to use
  schema:
    description: The schema to use

targets:
  dev:
    # The default target uses 'mode: development' to create a development copy.
    # - Deployed resources get prefixed with '[dev my_user_name]'
    # - Any job schedules and triggers are paused by default.
    # See also https://docs.databricks.com/dev-tools/bundles/deployment-modes.html.
    mode: development
    default: true
    workspace:
      host: https://dbc-bda95dae-2975.cloud.databricks.com
    variables:
      catalog: databricks_dev
      schema: bronze
  QA:
    mode: production
    workspace:
      host: https://dbc-bda95dae-2975.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/nagarajuboini94@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/nagarajuboini94@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricks_QA
      schema: bronze
  prod:
    mode: production
    workspace:
      host: https://dbc-bda95dae-2975.cloud.databricks.com
      # We explicitly deploy to /Workspace/Users/nagarajuboini94@gmail.com to make sure we only have a single copy.
      root_path: /Workspace/Users/nagarajuboini94@gmail.com/.bundle/${bundle.name}/${bundle.target}
    variables:
      catalog: databricks_prod
      schema: bronze  
    permissions:
      - user_name: nagarajuboini94@gmail.com
        level: CAN_MANAGE
